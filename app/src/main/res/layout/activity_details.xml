<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:bind="http://schemas.android.com/apk/res-auto">

    <data>
        <variable
            name="model"
            type="com.example.superheros.model.SuperHero" />

        <variable
            name="imageUrl"
            type="String" />
    </data>

<ScrollView android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorPrimaryDark"
    >

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="@dimen/default_padding_X2"
            tools:context=".view.DetailsActivity">

            <ImageView
                android:id="@+id/image"
                android:layout_width="270dp"
                android:layout_height="270dp"
                android:layout_gravity="center"
                android:scaleType="centerInside"
                bind:profileImage="@{imageUrl}"/>

            <include
                android:id="@+id/biography_view"
                layout="@layout/layout_biography"
                bind:model="@{model}"/>

            <include
                android:id="@+id/appearance_view"
                layout="@layout/layout_appearance"
                bind:model="@{model}"/>

            <include
                android:id="@+id/powerstats_view"
                layout="@layout/layout_powerstats"
                bind:model="@{model}"/>

            <include
                android:id="@+id/work_view"
                layout="@layout/layout_work"
                bind:model="@{model}"/>

            <include
                android:id="@+id/connections_view"
                layout="@layout/layout_connections"
                bind:model="@{model}"/>

        </LinearLayout>
    </ScrollView>
</layout>